apiVersion: v1
kind: Service
metadata:
  name: {{ include "yuanchao-aws-handson-topic9-charts.fullname" . }}
  labels:
    {{- include "yuanchao-aws-handson-topic9-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yuanchao-aws-handson-topic9-charts.selectorLabels" . | nindent 4 }}
